// For Libraries running in DOM / Client environment.

{
	"extends": "./tsconfig.lib.json",

	"compilerOptions": {
		// targets and runtime types
		// for dom libraries.
		"target": "ES2022",
		"lib": ["ES2022", "DOM"]
	}
}
